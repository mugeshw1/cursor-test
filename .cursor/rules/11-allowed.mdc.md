# Rules: Always Allowed Commands

> These commands are pre-approved and can be executed without confirmation.

---

## 🟢 File System
```
ls *
cat *
echo *
pwd
mkdir *
touch *
cp *
find * -name *
xargs *
```

## 🟢 File Reading
```
Read(**)   # all files except those in 01-sensitive-files.md
```

## 🟢 Package Scripts
```
npm run *
pnpm run *
yarn run *
npm run build *
pnpm run build *
yarn build *
make *
```

## 🟢 Runtime
```
node *
curl *
wget *
ping *
nslookup *
dig *
```

## 🟢 Git (Safe)
```
git status
git log *
git diff *
git add *
git commit *
git checkout -b *
git branch *
git stash *
git fetch *
git show *
```

## 🟢 Process Inspection
```
ps *
top
htop
lsof *
pgrep *
tail *
head *
grep *
wc *
sort *
uniq *
awk *
jq *
```

## 🟢 System Info
```
which *
whoami
id
uname *
df *
du *
free *
uptime
date
crontab -l
```
